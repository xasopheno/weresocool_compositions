{ f: 65, l: 3/20, g: 1, p: 0, w: 1.6, h: 0.9, background_color: #01060a, rand_seed: 19 }

-- ════════════════════════════════════════════════════════════════════════
-- GAHU SPIRAL — the forest at night, built by the drums.
--
-- Same machinery as the nocturne: nothing here glows. One cold sky, and
-- every note an opaque body standing in front of it (`over` chains ending
-- in `| Opaque`, so coverage comes from geometry, not brightness — the
-- only way a dark shape can hide light in an additive engine).
--
-- But the city is gone. The bodies here GREW, and a jungle has no
-- skyline: it has a floor, a canopy, and everything between them
-- reaching for the one gap in the leaves.
--
-- THE LAW: ACROSS IS WHEN, UP IS WHO.
--
-- Every voice in a Gahu ensemble plays ONE pitch and sits at ONE pan, so
-- neither pitch nor pan can place anything. What every stroke does have
-- is a POSITION IN THE BAR, and that is the x axis: the bell's sixteen
-- strokes are 2.4 seconds, and a mark lands at
--
--     x = (t / 2.4 mod 1) across the frame
--
-- so the frame is read left to right as one turn of the bell, and every
-- voice writes into the same ruler. A stroke is a column of light.
--
-- The parts are not all sixteen long — kidi is twelve, sogo and the
-- anchor are twenty-four — so against that ruler they DRIFT, a little
-- further right or left each turn, and take whole cycles to come back.
-- That drift is the spiral in the title, and it is now the thing you
-- watch: where two voices line up, two columns light at once.
--
-- AND EACH VOICE HOLDS FOR EXACTLY ONE TURN OF ITS OWN CYCLE. `Die` is
-- in l-units and one l-unit is one stroke, so the number IS the length of
-- the part — bell 16, kaganu 16, axatse 16, kidi 12, sogo 24, anchor 24.
-- Each band therefore carries one complete statement of its part, laid
-- out left to right and refreshing continuously; before this every voice
-- faded on a number picked by eye, so the frame only ever showed part of
-- a pattern, and a pattern you only ever see two thirds of is one you
-- cannot learn. The parts that are not sixteen long slide against the
-- ones that are, a little further every turn — and that is the drift the
-- title is named for, now with a whole cycle on screen to see it against.
--
-- y is which voice, and it is the forest's own order — fireflies in the
-- air, flowers under them, fiddleheads uncurling below those, trees
-- rooted in the water, reeds on the bank, and the mushrooms and lilies
-- at the waterline and on the stream itself. So the picture is a jungle read
-- top to bottom and a bar read left to right at the same time.
--
--   anchor (kick) → the TRUNKS. Each kick raises one, out of the stream,
--                   tapered, leaning, buttressed, forking near the top.
--   sogo   (low)  → FLOWERS: a five-petal night bloom that sweeps open
--                   from its heart on every stroke of the low drum.
--   kidi   (mid)  → FIDDLEHEADS: young ferns still furled, copper, the
--                   only spiral in the piece and findable at any size.
--   kaganu (high) → REEDS: four bright stems standing off the bank on
--                   every stroke — straight where the flower is round.
--   axatse (shk)  → THE EDGE OF THE BED, and it plays two species: the
--                   loud stroke stands MUSHROOMS on the damp ground, the
--                   ghost presses a ROSETTE flat to the sand with its
--                   flower beside it. The one voice on every stroke, so
--                   its accent pattern is which thing is growing, not how
--                   bright anything is.
--   bell   (gank) → FIREFLIES. The bell is the clock the whole ensemble
--                   hears; the fireflies are the only marks that drift.
--   bass          → THE SAND BED in the foreground. Its pitch is how far
--                   out each drift line lies, so the bassline is the bed.
--
-- Gain is the only other thing that varies (`Gm 0.5`…`Gm 1`), so gain is
-- weight everywhere: how tall the tree, how wide the flower, how much
-- litter. Nothing is keyed to a field that does not move.
--
-- AUDIO UNCHANGED — every added op is Color / wgsl / draw / warp.
-- ════════════════════════════════════════════════════════════════════════

-- THE LIGHT IS THE NIGHT THIS PIECE ALREADY HAS.
--
--   moon    the one hard source, high and to the right, cold. It is the
--           only thing in the frame that is far away.
--   canopy  the ambient: filtered green. A shadow in a jungle is not a
--           darkness, it is the colour of the leaf that made it.
--   floor   from BELOW — the moon coming back up off the pale sand of
--           the bed, faint and warm. It reaches the near plane and
--           nothing else, which is what makes it read as ground and not
--           as a second moon.
--
-- The far planes say so by name: reeds, fiddleheads, flowers, mushrooms
-- `Shade moon` — at that distance nothing else gets to them. The trunks
-- and the stream say nothing and so take every light, the floor among them.
light moon   = { from: (0.42, 0.92, 0.34), color: #cfe2ff, gain: 1.45 }
light canopy = { ambient, color: #16402c, gain: 0.72 }
light floor  = { from: (0.0, -1, 0.55), color: #ffddab, gain: 0.30 }

-- ── THE SKY UNDER THE CANOPY ────────────────────────────────────────────
-- The only emitter in the piece. It is a sky you can barely see: the moon
-- is up there, and the canopy is between. Two octaves of slow leaf noise
-- are the MASK — where the leaves are thin the light gets through, and
-- because the mask also breaks the shafts, the rays move when the leaves
-- do and not on a clock of their own. Flares on every kick.
layer {
    Raw {
        // the moon, high and to the right
        let a = vec2<f32>((uv.x - 0.72) * 1.78, uv.y - 0.13);
        let dm = length(a);
        let flare = 1.0;
        let disc = exp(0.0 - dm * dm * 820.0);
        let halo = exp(0.0 - dm * 3.10) * flare;

        // THE WIND, and everything below reads it
        let _w = 0.62 * sin(Time * 0.113) + 0.38 * sin(Time * 0.071 + 1.7);

        // THE CANOPY. Not drawn — a mask. It thins toward the bottom of
        // the frame because below the branches there is nothing to block.
        let d1 = k_vnoise(uv * vec2<f32>(9.0, 5.0) + vec2<f32>(Time * 0.013 + _w * 0.016, Time * 0.007));
        let d2 = k_vnoise(uv * vec2<f32>(23.0, 13.0) + vec2<f32>(Time * 0.021 + _w * 0.024, 0.0 - Time * 0.011));
        let leafy = clamp(d1 * 0.66 + d2 * 0.34, 0.0, 1.0);
        let cover = mix(1.0, smoothstep(0.16, 0.74, leafy), smoothstep(0.66, 0.14, uv.y));

        // SHAFTS — the same noise, so the light moves when the leaves do
        let ang = atan2(a.y, a.x);
        let ray = 0.5 + 0.5 * sin(ang * 17.0 + Time * 0.05 + leafy * 3.4 + _w * 0.62);
        let shaft = pow(ray, 5.0) * exp(0.0 - dm * 1.35) * smoothstep(0.02, 0.58, uv.y);

        let silver = vec3<f32>(0.80, 0.88, 1.00);
        let jade   = vec3<f32>(0.0060, 0.0270, 0.0180);
        let ink    = vec3<f32>(0.0016, 0.0052, 0.0040);

        // a forest has a TOP and a BOTTOM: cold and open above, black and
        // close below. That vertical range is most of what makes it read
        // as under something rather than out in the open.
        let up = clamp(1.0 - uv.y * 1.10, 0.0, 1.0);
        var c = mix(ink, jade, clamp(up * 1.25, 0.0, 1.0));
        c = c + silver * halo * 0.115 * cover;
        c = c + silver * disc * 1.70;
        c = c + vec3<f32>(0.40, 0.62, 0.80) * shaft * 0.085 * cover;

        // MIST, lying on the water and breathing
        let mp = (uv.y - 0.68) * 4.2;
        c = c + vec3<f32>(0.013, 0.024, 0.030) * exp(0.0 - mp * mp)
              * (0.55 + 0.45 * sin(Time * 0.09 + uv.x * 3.1));

        // and the light the sand throws back up out of the bed
        let fl = clamp((uv.y - 0.80) * 4.4, 0.0, 1.0);
        c = c + vec3<f32>(0.052, 0.040, 0.026) * fl * fl
              * (0.70 + 0.30 * sin(Time * 0.23 + uv.x * 5.0));

        // the night deepens across the piece. The clock is the music's.
        let deep = smoothstep(3.0, 30.0, Time);
        c = c * mix(vec3<f32>(1.0), vec3<f32>(0.62, 0.80, 0.92), deep);

        let dv = length(vec2<f32>((uv.x - 0.5) * 1.5, uv.y - 0.5));
        c = c * (1.0 - 0.80 * clamp(dv - 0.16, 0.0, 1.0));
        c = c * (1.0 - 0.07 * k_vnoise(uv * 220.0));
        color = vec4<f32>(c, 1.0);
    }
}

-- ── the planes, far to near (definition order IS depth order) ───────────

-- The pods, hanging under the canopy. Far enough back that the mist has
-- some of them — distance is a colour here as much as a size — but not so
-- far that the shape goes.
warp thicket over = {
    Scene
    | Blur 0.35
    | Gain 0.80
    | Opaque
    | Raw {
        let low = smoothstep(0.30, 0.88, uv.y);
        let fog = vec3<f32>(0.12, 0.14, 0.22);
        let _fogged = mix(color.rgb, fog * 0.80, 0.18 * (1.0 - low) + 0.05);
        // MOONSHINE: the broad turn toward the light, and the glint the
        // wet surface makes on top of it. The alpha is this plane's own
        // coverage, so the shine lands on the plant and never on the
        // night between them.
        let _wind = 0.62 * sin(Time * 0.113) + 0.38 * sin(Time * 0.071 + 1.7);
        let _mp = vec2<f32>((uv.x - 0.72 - _wind * 0.020) * 1.78, uv.y - 0.13 + _wind * 0.008);
        let _face = exp(0.0 - length(_mp) * 0.80);
        let _gl = k_vnoise(uv * 210.0);
        let _spec = pow(clamp(_gl, 0.0, 1.0), 4.0);
        let _sh = vec3<f32>(0.62, 0.76, 1.00) * (_face * 0.10 + _spec * _face * 0.30) * color.a;
        color = vec4<f32>(_fogged + _sh, color.a);
    }
}

-- MIDDLE: the flowers. This is the nocturne's window trick with the
-- windows taken out — procedural detail living INSIDE the silhouette,
-- masked by the mark's own coverage. What a night flower has instead of
-- windows is WATER: every petal in this forest is wet, and the moon
-- finds the film before it finds the flower. The dew fires on the sogo
-- hit, so the whole canopy glints on the low drum.
warp blooms over = {
    Scene
    | Tint(0.62, 0.52, 0.66)
    | Opaque
    | Raw {
        // a slow sheen crawling across the petals
        let _wsh = 0.62 * sin(Time * 0.113) + 0.38 * sin(Time * 0.071 + 1.7);
        let sheen = 0.5 + 0.5 * sin(uv.x * 5.4 + uv.y * 3.1 + Time * 0.24 + _wsh * 1.10);
        let band = pow(sheen, 6.0);
        // and dew, which only glints where the sheen is on it
        let cell = uv * vec2<f32>(150.0, 84.0);
        let id = floor(cell);
        let g = fract(cell) - vec2<f32>(0.5);
        let n = k_vnoise(id);
        let dew = (1.0 - smoothstep(0.06, 0.20, length(g))) * step(0.90, n);
        let twk = 0.55 + 0.45 * sin(Time * 2.3 + n * 40.0);
        var c = color.rgb;
        c = c + vec3<f32>(0.30, 0.44, 0.52) * band * 0.30 * color.a;
        c = c + vec3<f32>(0.75, 0.92, 1.00) * dew * twk * band * 1.40 * color.a;
        // MOONSHINE: the broad turn toward the light, and the glint the
        // wet surface makes on top of it. The alpha is this plane's own
        // coverage, so the shine lands on the plant and never on the
        // night between them.
        let _wind = 0.62 * sin(Time * 0.113) + 0.38 * sin(Time * 0.071 + 1.7);
        let _mp = vec2<f32>((uv.x - 0.72 - _wind * 0.020) * 1.78, uv.y - 0.13 + _wind * 0.008);
        let _face = exp(0.0 - length(_mp) * 0.80);
        let _gl = k_vnoise(uv * 300.0);
        let _spec = pow(clamp(_gl, 0.0, 1.0), 4.0);
        let _sh = vec3<f32>(0.62, 0.76, 1.00) * (_face * 0.22 + _spec * _face * 0.80) * color.a;
        color = vec4<f32>(c + _sh, color.a);
    }
}

-- NEAR: the trunks. Almost black, and the only bodies in the piece with a
-- surface — bark, which is vertical because they are, and fungus, which
-- is not: it sits in patches, breathes on its own hashed phase, and a
-- kick sends a ring of it racing outward through the wood.
warp boles over = {
    Scene
    | Tint(0.055, 0.075, 0.062)
    | Opaque
    | Raw {
        let fib = k_vnoise(vec2<f32>(uv.x * 520.0, uv.y * 26.0));
        let fib2 = k_vnoise(vec2<f32>(uv.x * 190.0, uv.y * 9.0));
        let bark = 0.72 + 0.46 * (fib * 0.6 + fib2 * 0.4);

        let pid = floor(uv * vec2<f32>(46.0, 26.0));
        let pn = k_vnoise(pid);
        let live = step(0.88, pn);
        let pf = fract(uv * vec2<f32>(46.0, 26.0)) - vec2<f32>(0.5);
        let blot = (1.0 - smoothstep(0.10, 0.42, length(pf))) * live;
        let puls = 0.45 + 0.55 * sin(Time * 1.4 + pn * 61.0);

        var c = color.rgb * bark;
        c = c + vec3<f32>(0.14, 0.62, 0.40) * blot * puls * 0.55 * color.a;
        // MOONSHINE: the broad turn toward the light, and the glint the
        // wet surface makes on top of it. The alpha is this plane's own
        // coverage, so the shine lands on the plant and never on the
        // night between them.
        let _wind = 0.62 * sin(Time * 0.113) + 0.38 * sin(Time * 0.071 + 1.7);
        let _mp = vec2<f32>((uv.x - 0.72 - _wind * 0.020) * 1.78, uv.y - 0.13 + _wind * 0.008);
        let _face = exp(0.0 - length(_mp) * 0.80);
        let _gl = k_vnoise(uv * 260.0);
        let _spec = pow(clamp(_gl, 0.0, 1.0), 4.0);
        let _sh = vec3<f32>(0.62, 0.76, 1.00) * (_face * 0.030 + _spec * _face * 0.15) * color.a;
        color = vec4<f32>(c + _sh, color.a);
    }
}

-- The fireflies, in front of the trees. These are the one thing in the
-- piece that is allowed to be a light instead of a body — no `Opaque`, so
-- they add rather than cover, which is what a small lamp does.
warp glow over = {
    Scene
    | Bloom(0.22, 1.30)
    | Gain 1.10
}

-- The bank: the reeds standing between the water and the trees.
warp bank over = {
    Scene
    | Gain 0.85
    | Opaque
    | Raw {
        // MOONSHINE: the broad turn toward the light, and the glint the
        // wet surface makes on top of it. The alpha is this plane's own
        // coverage, so it lands on the plant and not on the night.
        let _wind = 0.62 * sin(Time * 0.113) + 0.38 * sin(Time * 0.071 + 1.7);
        let _mp = vec2<f32>((uv.x - 0.72 - _wind * 0.020) * 1.78, uv.y - 0.13 + _wind * 0.008);
        let _face = exp(0.0 - length(_mp) * 0.80);
        let _gl = k_vnoise(uv * 280.0);
        let _spec = pow(clamp(_gl, 0.0, 1.0), 4.0);
        let _sh = vec3<f32>(0.62, 0.76, 1.00) * (_face * 0.20 + _spec * _face * 0.75) * color.a;
        color = vec4<f32>(color.rgb + _sh, color.a);
    }
}

-- FOREGROUND: THE SAND. Defined last, so it lies in front of everything
-- — you are standing on the near bank of a dry bed.
--
-- It used to be a stream, and the difference between the two is entirely
-- in how they take the light. Water REFLECTS: it borrowed the trunks'
-- own image, mirrored about the waterline, and broke the moon into
-- caustics on top of it. Sand does the opposite — it is matte, it gives
-- nothing back, and the only thing you see is its own RELIEF: the ripples
-- the wind leaves lying across the bed, lit on the faces that turn to the
-- moon and shadowed on the ones that turn away.
--
-- So the reflection is gone, the caustics are gone, and what is left is a
-- lambert term against the slope of the ripple field plus the grain of
-- the grains themselves. The bass still draws the bed — its pitch is
-- still how far out each drift line lies — but the marks are now dry
-- drift rather than current.
warp sand over = {
    Scene
    | Blur 0.50
    | Tint(0.38, 0.33, 0.25)
    | Opaque
    | Raw {
        let _wind = 0.62 * sin(Time * 0.113) + 0.38 * sin(Time * 0.071 + 1.7);
        let h = 0.70;
        if (uv.y > h) {
            let d = uv.y - h;
            // THE RIPPLES. They run ACROSS the bed, and their line is bent
            // by a slow function of x, which is what stops a ripple field
            // reading as a barcode. The phase creeps with the wind.
            let ph = uv.y * 190.0 + sin(uv.x * 2.7 + _wind * 0.45) * 2.4
                   + sin(uv.x * 7.3) * 0.7;
            // the derivative IS the slope, so the lit face falls out of
            // the same expression that drew the ripple — no second field,
            // and the light can never disagree with the shape
            let dr = cos(ph);
            let ph2 = uv.y * 61.0 - uv.x * 1.9 + _wind * 0.30;
            let dr2 = cos(ph2);
            let lit = clamp(0.52 + 0.34 * dr + 0.16 * dr2, 0.0, 1.2);
            // grain: fine near your feet, finer as the bed runs away
            let gr = k_vnoise(uv * (620.0 + 900.0 * d));
            // AND IT GLOWS. Matte is not the same as dark: pale sand under
            // a moon holds light and hands it back diffusely, which is the
            // whole reason a beach at night is the brightest thing on it.
            // The sheen is broad and soft where the water's was tight and
            // hard — but it is not weak.
            let sh = exp(0.0 - length(vec2<f32>((uv.x - 0.72 - _wind * 0.02) * 1.78, uv.y - 0.13)) * 0.40);
            var c = color.rgb * (0.34 + 1.45 * lit);
            c = c * (0.84 + 0.32 * gr);
            c = c + vec3<f32>(0.44, 0.38, 0.29) * sh * 0.44 * color.a;
            // and the bed's own light, following the same ripple term, so
            // the crests carry the glow and the troughs stay dark
            c = c + vec3<f32>(0.115, 0.098, 0.072) * color.a * (0.30 + 0.70 * lit);
            // the bed darkens into the near corner, out of the moon
            c = c * (1.0 - 0.18 * clamp(d * 1.5 - 0.14, 0.0, 1.0));
            color = vec4<f32>(c, color.a);
        }
    }
    -- and a little bloom, so the lit crests bleed the way a bright
    -- diffuse surface does
    | Bloom(0.34, 0.50)
}

-- THE EDGE OF THE BED: the mushrooms and the rosettes, in front of
-- everything. Opaque, because they are bodies — a cap is solid and a
-- rosette is a leaf on the ground, and both hide what is under them.
warp shallows over = {
    Scene
    | Gain 1.05
    | Opaque
    | Raw {
        // MOONSHINE: the broad turn toward the light, and the glint the
        // wet surface makes on top of it. The alpha is this plane's own
        // coverage, so the shine lands on the plant and never on the
        // night between them.
        let _wind = 0.62 * sin(Time * 0.113) + 0.38 * sin(Time * 0.071 + 1.7);
        let _mp = vec2<f32>((uv.x - 0.72 - _wind * 0.020) * 1.78, uv.y - 0.13 + _wind * 0.008);
        let _face = exp(0.0 - length(_mp) * 0.80);
        let _gl = k_vnoise(uv * 320.0);
        let _spec = pow(clamp(_gl, 0.0, 1.0), 4.0);
        let _sh = vec3<f32>(0.62, 0.76, 1.00) * (_face * 0.26 + _spec * _face * 1.00) * color.a;
        color = vec4<f32>(color.rgb + _sh, color.a);
    }
}

-- ── THE FIGURES ─────────────────────────────────────────────────────────
-- NOTE ON UNITS. Draw space is y-up; the waterline is y = 0.30, which the
-- stream's Raw knows as uv.y = 0.70. Every figure is built with its base
-- at the origin and then pinned: `Ya(seat - Note.y)` cancels the note's
-- own pitch, which is CONSTANT in every drum voice here, so all of them
-- land on the same ground line. `Xa(px)` does NOT cancel pan — the pan is
-- allowed to nudge a figure sideways, so the stereo field is the width of
-- the forest.
--
-- Every shape uses one Spawn and decomposes `Stroke` into two parameters:
-- `floor(Stroke * N)` is which rib/band, `fract(Stroke * N)` is where
-- along it. One expansion, two dimensions, no nesting.

-- THE FRAME, MEASURED OFF THE RENDER (which is not what the reference
-- says, and the render is the authority): a pinned mark at world y lands
-- at screen fraction f = 0.90 - 0.36 * y, and at x it lands at
-- f = 0.50 + 0.26 * x. So the visible window is
--
--     x in [-1.92, 1.92]      y in [-0.28, 2.50]
--
-- +y up, -z recedes and shrinks. The camera is NOT centred on the origin:
-- the middle of the frame is y = 1.11, which is why the first pinned pass
-- put the whole forest off the bottom edge. Every number below is derived
-- from those two lines.
-- Every figure below is built directly in those coordinates and then
-- `Pin`ned, which makes the set ABSOLUTE — it stops riding the note's pan
-- and pitch. That matters here more than in most pieces: a Gahu drum
-- plays ONE pitch and sits at ONE pan, so riding them would pile every
-- mark of a voice on a single point. Pinned, the note's INDEX is free to
-- do the placing, which is the law this piece is built on.
--
-- The waterline is y = 0.556, which is uv.y = 0.70 — the same line the
-- stream's Raw mirrors its reflection about.
--
-- And a stamp lives 12 s at most (the pool retires it), so no figure can
-- outlast that: the forest is always the last twelve seconds of playing.
-- A kick therefore plants a GROVE, not a tree — five at once, each with
-- its own height, lean, width and place hashed from the note's index and
-- the tree's own number. One tree and one sapling a kick, both on the
-- kick's own bar column, standing for 3.6 s — one full turn of the
-- anchor, so the last grove clears exactly as the same stroke comes round
-- again. Four strokes to the anchor's twenty-four, so four columns of
-- trunk at any moment, and their HEIGHTS are the four gains the anchor
-- plays (1, 0.7, 0.8, 0.6) in that order. The part is the picture.

-- ANCHOR → a grove. Each tree rises out of the stream, tapering as it
-- climbs, leaning the way its hash leans, buttressed at the water, and
-- forking six ways near the top. `Stagger` orders the bands bottom-first,
-- so they GROW rather than appear.
draw trunk = {
    let dth  = Note.t * 61.7 - floor(Note.t * 61.7)
    let dt2  = Note.t * 37.3 - floor(Note.t * 37.3)
    let base = 0.556

    -- ── the boles: 2 trees x 30 bands x 6 across = 360 emits
    let bidx = floor(Stroke * 359.0 + 0.5)
    let bt   = floor(bidx / 180.0)
    let brem = bidx - bt * 180.0
    let bb   = floor(brem / 6.0)
    let bc   = brem - bb * 6.0
    let bv   = (bb + dth) / 30.0
    let bu   = (bc + dt2) / 6.0

    -- ACROSS IS WHEN: the bar is 16 strokes of 0.15 s, and where the
    -- kick falls in it is where its trees stand.
    let bph  = Note.t / 2.4 - floor(Note.t / 2.4)
    let bs   = Count * 0.61803 + bt * 0.31830
    let b1   = bs - floor(bs)
    let bs2  = bs * 3.3301
    let b2   = bs2 - floor(bs2)
    let bs3  = bs * 7.7177
    let b3   = bs3 - floor(bs3)

    let bpx  = bph * 2.70 - 1.35 + bt * 0.075
    let bh   = (0.98 - bt * 0.30 + b2 * 0.16) * (0.30 + Note.z * 1.05)
    let blen = b3 * 0.34 - 0.17
    let bwid = (0.0130 + b2 * 0.0115) * (0.72 + Note.z * 0.46)
    let btap = 1.0 - 0.58 * bv
    let bbut = 0.021 / (1.0 + bv * 18.0)
    let bswy = sin(bv * 5.1 + b3 * 6.2831) * 0.019 * bv

    -- ── the limbs: 2 trees x 6 limbs x 13 along = 156 emits
    let lidx = floor(Stroke * 155.0 + 0.5)
    let lt   = floor(lidx / 78.0)
    let lrem = lidx - lt * 78.0
    let lb   = floor(lrem / 13.0)
    let lq   = (lrem - lb * 13.0 + dth) / 13.0

    let lph  = Note.t / 2.4 - floor(Note.t / 2.4)
    let ls   = Count * 0.61803 + lt * 0.31830
    let l1   = ls - floor(ls)
    let ls2  = ls * 3.3301
    let l2   = ls2 - floor(ls2)
    let ls3  = ls * 7.7177
    let l3   = ls3 - floor(ls3)

    let lpx  = lph * 2.70 - 1.35 + lt * 0.075
    let lh   = (0.98 - lt * 0.30 + l2 * 0.16) * (0.30 + Note.z * 1.05)
    let llen = l3 * 0.34 - 0.17
    let ldir = 0.0 - 1.0 + 2.0 * Wrap(lb, 2.0)
    let lhi  = floor(lb / 2.0)
    let lspr = 0.55 + Wrap(lb * 7.0, 3.0) * 0.28
    let lv   = 0.70 + lhi * 0.12
    let lx0  = lpx + llen * lv * lv + sin(lv * 5.1 + l3 * 6.2831) * 0.019 * lv

    Point
    | Overlay [
        Spawn { n: 360, by: Sm(1.0) }
            | Xa(bpx + blen * bv * bv + bswy + (bu - 0.5) * 2.0 * (bwid * btap + bbut))
            | Ya(base + bv * bh),
        Spawn { n: 156, by: Sm(1.0) }
            | Xa(lx0 + ldir * lq * (0.080 + Note.z * 0.072) * lspr)
            | Ya(base + lv * lh + lq * lh * (0.24 - lhi * 0.05) - lq * lq * lh * 0.11),
      ]
    | Jitter(0.0009)
    | Stagger { over: 0.07 }
    | Pin
    | Gradient
    | Tint(1.0, 1.0, 0.98, 0.90)
}

-- SOGO → a night flower. A corolla of five petals opening from the heart
-- outward: the index runs round the rim, and `Stagger` lays it in that
-- order, so the flower does not appear — it SWEEPS open like a second
-- hand, once per sogo stroke. Five of them, and no two alike: petal
-- count, openness, size, height and hue are all hashed per flower.
--
-- The petal outline is the polar rose |cos(n·θ/2)|, lifted off zero so
-- the lobes stay joined at the heart instead of pinching to points, and
-- the whole figure is rotated by its own hash so no two open alike.
draw bloom = {
    let dth  = Note.t * 61.7 - floor(Note.t * 61.7)
    let fph  = Note.t / 2.4 - floor(Note.t / 2.4)

    let na   = 36.0
    let nr   = 8.0
    let idx  = floor(Stroke * 1439.0 + 0.5)
    let fi   = floor(idx / 288.0)
    let rem  = idx - fi * 288.0
    let ai   = floor(rem / nr)
    let qi   = rem - ai * nr

    let fs   = Count * 0.38197 + fi * 0.61803
    let f1   = fs - floor(fs)
    let fs2  = fs * 3.3301
    let f2   = fs2 - floor(fs2)
    let fs3  = fs * 7.7177
    let f3   = fs3 - floor(fs3)

    let px   = fph * 2.70 - 1.35 + (fi - 2.0) * 0.115 + (f1 - 0.5) * 0.62
    let py   = 0.46 + f2 * 1.62
    -- four to eight petals, hashed per flower: |cos(n·θ/2)| is the
    -- n-petal polar rose, and n is the one number that changes what a
    -- flower IS rather than how big it is
    let np   = 4.0 + floor(f3 * 5.0)
    let siz  = (0.040 + f2 * 0.165) * (0.60 + Note.z * 0.80)
    let spin = f3 * 6.28318
    let tha  = (ai + dth * 0.7) / na * 6.28318
    let lobe = abs(cos(tha * np * 0.5))
    -- and how far the petals are cut back toward the heart: 0.10 is a
    -- star, 0.55 is a cup
    let cutb = 0.10 + f1 * 0.45
    let rr   = siz * (cutb + (1.0 - cutb) * pow(lobe, 0.40 + f1 * 0.55))
    let qf   = (qi + dth) / nr
    let rad  = rr * qf
    let th   = tha + spin

    Point
    | Spawn { n: 1440, by: Sm(1.0) }
        | Xa(px + sin(th) * rad)
        -- 1.28 is the aspect: one x unit covers more of the frame than
        -- one y unit, so a circle needs the taller radius
        | Ya(py + cos(th) * rad * 1.28)
    | Jitter(0.0010)
    | Stagger { over: 0.14 }
    | Pin
    | Gradient(moon)
    -- gold at the heart, moon-white at the rim
    -- and its own colour: the heart stays gold, the rim wanders from
    -- cream through rose to lilac depending on the flower's hash
    | Tint(1.0,
           (0.72 + 0.28 * qf) * (0.74 + f3 * 0.26),
           (0.30 + 0.70 * qf) * (0.52 + f1 * 0.48),
           0.85)
}

-- KAGANU → reeds. The blossom had to go: it was the sogo's flower at a
-- fifth the size, and two voices sharing one figure is two voices you
-- cannot tell apart. A reed is the opposite of a flower in every way that
-- reads at a glance — straight where the flower is round, rising where
-- the vine falls, and the only bright green in the frame.
--
-- Four to a stroke, standing off the bank on the kaganu's own bar column.
-- Each is a stem that leans and thins, with two blades springing off it.
draw reed = {
    let dth  = Note.t * 61.7 - floor(Note.t * 61.7)
    let nph  = Note.t / 2.4 - floor(Note.t / 2.4)

    -- ── stems: 4 reeds x 30 along x 2 across = 240 emits
    let sidx = floor(Stroke * 239.0 + 0.5)
    let si   = floor(sidx / 60.0)
    let srem = sidx - si * 60.0
    let st   = (floor(srem / 2.0) + dth) / 30.0
    let sc   = srem - floor(srem / 2.0) * 2.0

    let ss   = Count * 0.38197 + si * 0.61803
    let s1   = ss - floor(ss)
    let ss2  = ss * 3.3301
    let s2   = ss2 - floor(ss2)
    let ss3  = ss * 7.7177
    let s3   = ss3 - floor(ss3)

    let spx  = nph * 2.70 - 1.35 + (si - 1.5) * 0.085
    let spy  = 0.40 + s2 * 0.13
    let sh   = (0.16 + s2 * 0.52) * (0.60 + Note.z * 1.05)
    let sln  = (s3 - 0.5) * (0.06 + s1 * 0.26)
    let swd  = 0.0055 * (1.0 - 0.75 * st)

    -- ── blades: 4 reeds x 2 blades x 14 along = 112 emits
    let bidx = floor(Stroke * 111.0 + 0.5)
    let bi   = floor(bidx / 28.0)
    let brem = bidx - bi * 28.0
    let bd2  = floor(brem / 14.0)
    let bq   = (brem - bd2 * 14.0 + dth) / 14.0

    let bs   = Count * 0.38197 + bi * 0.61803
    let b1   = bs - floor(bs)
    let bs2  = bs * 3.3301
    let b2   = bs2 - floor(bs2)
    let bs3  = bs * 7.7177
    let b3   = bs3 - floor(bs3)

    let bpx  = nph * 2.70 - 1.35 + (bi - 1.5) * 0.085
    let bpy  = 0.40 + b2 * 0.13
    let bh   = (0.16 + b2 * 0.52) * (0.60 + Note.z * 1.05)
    let bln  = (b3 - 0.5) * (0.06 + b1 * 0.26)
    let bdir = 0.0 - 1.0 + 2.0 * bd2
    let bv   = 0.20 + bd2 * (0.18 + b1 * 0.42)

    Point
    | Overlay [
        Spawn { n: 240, by: Sm(1.0) }
            | Xa(spx + sln * st * st + (sc - 0.5) * 2.0 * swd)
            | Ya(spy + st * sh),
        Spawn { n: 112, by: Sm(1.0) }
            | Xa(bpx + bln * bv * bv + bdir * bq * (0.028 + b3 * 0.095))
            | Ya(bpy + bv * bh + bq * bh * (0.18 + b1 * 0.34) - bq * bq * bh * (0.24 + b3 * 0.40)),
      ]
    | Jitter(0.0008)
    | Pin
    | Gradient(moon)
    | Tint(0.80, 1.0, 0.86, 0.90)
}

-- KIDI → fiddleheads. A young fern before it opens: a stalk that leans
-- out and then rolls its tip into a tight coil. It is the only SPIRAL in
-- the piece, which is the reason it is here — a spiral is findable at any
-- size, at any brightness, next to anything else, and kidi needed to stop
-- being confused with the reeds standing under it.
--
-- Four to a stroke on kidi's own bar column, in copper, standing in the
-- middle ground between the reeds on the bank and the flowers above.
draw crozier = {
    let dth = Note.t * 61.7 - floor(Note.t * 61.7)
    let vph = Note.t / 2.4 - floor(Note.t / 2.4)

    -- ── stalks: 4 x 26 along x 2 across = 208 emits
    let sidx = floor(Stroke * 207.0 + 0.5)
    let si   = floor(sidx / 52.0)
    let srem = sidx - si * 52.0
    let su   = (floor(srem / 2.0) + dth) / 26.0
    let sc   = srem - floor(srem / 2.0) * 2.0

    let ss   = Count * 0.61803 + si * 0.38197
    let s1   = ss - floor(ss)
    let ss2  = ss * 3.3301
    let s2   = ss2 - floor(ss2)
    let ss3  = ss * 7.7177
    let s3   = ss3 - floor(ss3)

    let spx  = vph * 2.70 - 1.35 + (si - 1.5) * 0.105 + (s1 - 0.5) * 0.035
    let spy  = 0.50 + s2 * 0.12
    let sh   = (0.26 + s2 * 0.62) * (0.60 + Note.z * 0.95)
    let sdir = 0.0 - 1.0 + 2.0 * floor(s3 * 2.0)
    let sln  = sdir * (0.020 + s3 * 0.140)
    let swd  = 0.0055 * (1.0 - 0.45 * su)

    -- ── coils: 4 x 64 round = 256 emits. Two and a bit turns, closing
    --    on the centre — the head of the fern, still furled.
    let cidx = floor(Stroke * 255.0 + 0.5)
    let ci   = floor(cidx / 64.0)
    let cw   = ((cidx - ci * 64.0) + dth) / 64.0

    let cs   = Count * 0.61803 + ci * 0.38197
    let c1   = cs - floor(cs)
    let cs2  = cs * 3.3301
    let c2   = cs2 - floor(cs2)
    let cs3  = cs * 7.7177
    let c3   = cs3 - floor(cs3)

    let cpx  = vph * 2.70 - 1.35 + (ci - 1.5) * 0.105 + (c1 - 0.5) * 0.035
    let cpy  = 0.50 + c2 * 0.12
    let ch   = (0.26 + c2 * 0.62) * (0.60 + Note.z * 0.95)
    let cdir = 0.0 - 1.0 + 2.0 * floor(c3 * 2.0)
    let crad = (0.016 + c2 * 0.050) * (0.60 + Note.z * 0.95)
    -- the coil sits off the top of the stalk, on the side it leans to
    let ccx  = cpx + cdir * (0.020 + c3 * 0.140) + cdir * crad
    let ccy  = cpy + ch + crad * 1.28
    -- one and a half turns to three and a half, and closing anywhere
    -- from a loose curl to a tight button
    let cang = 3.14159 + cdir * cw * (9.0 + c1 * 13.0)
    let crr  = crad * (1.0 - (0.62 + c3 * 0.34) * cw)

    Point
    | Overlay [
        Spawn { n: 208, by: Sm(1.0) }
            | Xa(spx + sln * su * su + (sc - 0.5) * 2.0 * swd)
            | Ya(spy + su * sh),
        Spawn { n: 256, by: Sm(1.0) }
            | Xa(ccx + sin(cang) * crr)
            | Ya(ccy + cos(cang) * crr * 1.28),
      ]
    | Jitter(0.0007)
    | Pin
    | Gradient(moon)
    | Tint(1.0, 0.88, 0.72, 0.88)
}

-- AXATSE, ACCENT → mushrooms, standing on the wet ground at the water's
-- edge. They were arrowheads and they kept reading as little parasols,
-- which is a fair thing for a shape to tell you about itself — a dome on
-- a stalk IS a mushroom, so it may as well be one.
--
-- Three to a stroke on the axatse's own bar column. Each is built in
-- three parts, and every one of them varies:
--
--   the CAP is a dome sampled over its half-turn: `sin(a)^e` is the
--   profile, and `e` is the whole character of the species — under 1 it
--   is a flat parasol, at 1 a button, over 1 a little conical bell. The
--   rim HANGS BELOW the top of the stalk, which is the one detail that
--   stops a dome reading as a bead on a stick.
--
--   the GILLS are the underside, radiating from the stalk to the rim,
--   and they are what makes it read as fungus rather than as a lamp.
--
--   the STALK tapers upward and swells at the foot.
--
-- Cool bone-white going to a bioluminous green in the deep tones, since
-- the floor's own light is the only thing under there with them.
draw mushroom = {
    let dth = Note.t * 61.7 - floor(Note.t * 61.7)
    let cph = Note.t / 2.4 - floor(Note.t / 2.4)
    let siz = 0.45 + Note.z * 1.60

    -- ── caps: 3 x 28 over the dome x 8 deep = 672 emits
    let bidx = floor(Stroke * 671.0 + 0.5)
    let bi   = floor(bidx / 224.0)
    let brem = bidx - bi * 224.0
    let ba   = (floor(brem / 8.0) + dth) / 28.0
    let br   = (brem - floor(brem / 8.0) * 8.0 + dth) / 8.0

    let bs   = Count * 0.61803 + bi * 0.38197
    let b1   = bs - floor(bs)
    let bs2  = bs * 3.3301
    let b2   = bs2 - floor(bs2)
    let bs3  = bs * 7.7177
    let b3   = bs3 - floor(bs3)

    let bpx  = cph * 2.70 - 1.35 + (bi - 1.0) * (0.040 + b1 * 0.155) + (b1 - 0.5) * 0.06
    let bpy  = 0.20 + b2 * 0.26
    let bst  = (0.028 + b2 * 0.080) * siz
    let bcw  = (0.020 + b3 * 0.048) * siz
    let bch  = bcw * (0.42 + b1 * 1.15)
    let bang = ba * 3.14159
    -- under 1 a parasol, over 1 a bell: the species, in one number
    let bex  = 0.40 + b1 * 1.20
    let bdrp = bch * (0.16 + b3 * 0.34)
    let blo  = bpy + bst - bdrp * (1.0 - pow(sin(bang), 0.5))
    let bhi  = bpy + bst + pow(sin(bang), bex) * bch * 1.28
    -- spots: a hash per cell of the cap, only on the surface, and only on
    -- the caps whose own hash says this is a spotted kind
    let bhs  = sin(floor(ba * 28.0) * 12.9898 + floor(br * 8.0) * 78.233) * 43758.5453
    let bspt = bhs - floor(bhs)
    let bspot = clamp((bspt - 0.86) / 0.14, 0.0, 1.0) * br * clamp((b3 - 0.52) / 0.18, 0.0, 1.0)

    -- ── gills: 3 x 20 out x 3 deep = 180 emits
    let gidx = floor(Stroke * 179.0 + 0.5)
    let gi   = floor(gidx / 60.0)
    let grem = gidx - gi * 60.0
    let ga   = (floor(grem / 3.0) + dth) / 20.0
    let gr   = (grem - floor(grem / 3.0) * 3.0 + dth) / 3.0

    let gs   = Count * 0.61803 + gi * 0.38197
    let g1   = gs - floor(gs)
    let gs2  = gs * 3.3301
    let g2   = gs2 - floor(gs2)
    let gs3  = gs * 7.7177
    let g3   = gs3 - floor(gs3)

    let gpx  = cph * 2.70 - 1.35 + (gi - 1.0) * (0.040 + g1 * 0.155) + (g1 - 0.5) * 0.06
    let gpy  = 0.20 + g2 * 0.26
    let gst  = (0.028 + g2 * 0.080) * siz
    let gcw  = (0.020 + g3 * 0.048) * siz
    let gch  = gcw * (0.42 + g1 * 1.15)
    let gang = ga * 3.14159
    let gdrp = gch * (0.16 + g3 * 0.34)
    let gstripe = Wrap(floor(ga * 20.0), 2.0)

    -- ── stalks: 3 x 18 up x 3 across = 162 emits
    let sidx = floor(Stroke * 161.0 + 0.5)
    let si   = floor(sidx / 54.0)
    let srem = sidx - si * 54.0
    let su   = (floor(srem / 3.0) + dth) / 18.0
    let sv   = (srem - floor(srem / 3.0) * 3.0 + dth) / 3.0

    let ss   = Count * 0.61803 + si * 0.38197
    let s1   = ss - floor(ss)
    let ss2  = ss * 3.3301
    let s2   = ss2 - floor(ss2)
    let ss3  = ss * 7.7177
    let s3   = ss3 - floor(ss3)

    let spx  = cph * 2.70 - 1.35 + (si - 1.0) * (0.040 + s1 * 0.155) + (s1 - 0.5) * 0.06
    let spy  = 0.20 + s2 * 0.26
    let sst  = (0.028 + s2 * 0.080) * siz
    let slea = (s3 - 0.5) * 0.030 * siz
    -- narrows as it climbs, and swells at the foot
    let swd  = (0.0045 + s3 * 0.0055) * siz * (1.0 - 0.34 * su) * (1.0 + 0.85 / (1.0 + su * 14.0))

    -- ── skirts: 3 x 2 sides x 16 out = 96 emits
    let kidx = floor(Stroke * 95.0 + 0.5)
    let ki   = floor(kidx / 32.0)
    let krem = kidx - ki * 32.0
    let kd   = floor(krem / 16.0)
    let kq   = (krem - kd * 16.0 + dth) / 16.0

    let ks   = Count * 0.61803 + ki * 0.38197
    let k1   = ks - floor(ks)
    let ks2  = ks * 3.3301
    let k2   = ks2 - floor(ks2)
    let ks3  = ks * 7.7177
    let k3   = ks3 - floor(ks3)

    let kpx  = cph * 2.70 - 1.35 + (ki - 1.0) * (0.040 + k1 * 0.155) + (k1 - 0.5) * 0.06
    let kpy  = 0.20 + k2 * 0.26
    let kst  = (0.028 + k2 * 0.080) * siz
    let klea = (k3 - 0.5) * 0.030 * siz
    let kdir = 0.0 - 1.0 + 2.0 * kd
    let kv   = 0.52 + k1 * 0.22
    let kw   = (0.008 + k3 * 0.016) * siz
    -- only the ones past the threshold have torn a veil yet
    let kon  = clamp((k3 - 0.42) / 0.16, 0.0, 1.0)

    Point
    | Overlay [
        -- THE CAP: ochre, darker over the crown the way a real one is,
        -- and on the ones whose hash calls for it, spotted.
        Spawn { n: 672, by: Sm(1.0) }
            | Xa(bpx + cos(bang) * bcw)
            | Ya(blo + br * (bhi - blo))
            | Tint(1.02 - 0.26 * sin(bang) + bspot * 0.60,
                   (0.84 - 0.22 * sin(bang) + bspot * 0.90) * (0.86 + b3 * 0.22),
                   (0.52 - 0.20 * sin(bang) + bspot * 1.10) * (0.74 + b1 * 0.40),
                   0.92),
        -- THE GILLS: darker than the cap and alternating, because gills
        -- are a comb and a comb is read by its stripes
        Spawn { n: 180, by: Sm(1.0) }
            | Xa(gpx + cos(gang) * gcw * gr)
            | Ya(gpy + gst - gdrp * (1.0 - pow(sin(gang), 0.5)) * gr - 0.0035 * siz)
            | Tint(0.86 - gstripe * 0.26,
                   0.66 - gstripe * 0.24,
                   0.48 - gstripe * 0.18,
                   0.88),
        -- THE STALK: the palest thing on the mushroom
        Spawn { n: 162, by: Sm(1.0) }
            | Xa(spx + slea * su * su + (sv - 0.5) * 2.0 * swd)
            | Ya(spy + su * sst)
            | Tint(1.04, 1.00, 0.92, 0.90),
        -- THE SKIRT: the torn ring left where the veil broke, on the
        -- mushrooms old enough to have one
        Spawn { n: 96, by: Sm(1.0) }
            | Xa(kpx + klea * kv * kv + kdir * kq * kw)
            | Ya(kpy + kv * kst - kq * kq * kw * 1.10)
            | Tint(1.00, 0.94, 0.84, 0.72 * kon),
      ]
    | Jitter(0.0005)
    | Pin
    | Gradient(moon)
}

-- AXATSE, GHOST → lily pads, lying flat on the stream. The quiet stroke
-- of the same voice gets a different species, low and horizontal, so the
-- shaker's accent pattern is legible as TWO PLANTS rather than as two
-- brightnesses. The notch is what stops a pad reading as a circle: the
-- wedge collapses to the centre, the way a real pad is cut to its stem.
draw pad = {
    let dth = Note.t * 61.7 - floor(Note.t * 61.7)
    let gph = Note.t / 2.4 - floor(Note.t / 2.4)

    -- 3 pads x 26 round x 4 out = 312 emits
    let idx = floor(Stroke * 311.0 + 0.5)
    let gi  = floor(idx / 104.0)
    let rem = idx - gi * 104.0
    let ai  = floor(rem / 4.0)
    let qi  = rem - ai * 4.0
    let th  = (ai + dth) / 26.0 * 6.28318

    let gs  = Count * 0.61803 + gi * 0.38197
    let g1  = gs - floor(gs)
    let gs2 = gs * 3.3301
    let g2  = gs2 - floor(gs2)
    let gs3 = gs * 7.7177
    let g3  = gs3 - floor(gs3)

    let px  = gph * 2.70 - 1.35 + (gi - 1.0) * 0.135 + (g1 - 0.5) * 0.06
    let py  = 0.06 + g2 * 0.30
    let spin = g3 * 6.28318
    -- the notch: inside a narrow wedge the radius collapses to nothing
    let off = abs(th - 3.14159)
    let cut = clamp(off / (0.18 + g1 * 0.60), 0.0, 1.0)
    let rr  = (0.018 + g2 * 0.062) * (0.55 + Note.z * 1.40) * (0.12 + 0.88 * cut)
    let rad = rr * (qi + dth) / 4.0

    -- ── lilies: 3 x 30 round x 3 out = 270 emits, sitting just off the
    --    pad and standing a little proud of the surface
    let lidx = floor(Stroke * 269.0 + 0.5)
    let li   = floor(lidx / 90.0)
    let lrem = lidx - li * 90.0
    let lth  = (floor(lrem / 3.0) + dth) / 30.0 * 6.28318
    let lq   = (lrem - floor(lrem / 3.0) * 3.0 + dth) / 3.0

    let ls   = Count * 0.61803 + li * 0.38197
    let l1   = ls - floor(ls)
    let ls2  = ls * 3.3301
    let l2   = ls2 - floor(ls2)
    let ls3  = ls * 7.7177
    let l3   = ls3 - floor(ls3)

    let lpx  = gph * 2.70 - 1.35 + (li - 1.0) * 0.135 + (l1 - 0.5) * 0.06
    let lpy  = 0.06 + l2 * 0.30
    let lsz  = (0.009 + l2 * 0.026) * (0.55 + Note.z * 1.40)
    let lsid = 0.0 - 1.0 + 2.0 * floor(l3 * 2.0)
    -- six, eight or ten petals on the lily, and how deeply they are cut
    let lnp  = 3.0 + floor(l1 * 3.0)
    let lrr  = lsz * (0.20 + 0.80 * pow(abs(cos(lth * lnp)), 0.40 + l2 * 0.50)) * lq

    Point
    | Overlay [
        Spawn { n: 312, by: Sm(1.0) }
            | Xa(px + sin(th + spin) * rad)
            -- lying flat: a circle on the water is this squashed
            | Ya(py + cos(th + spin) * rad * (0.22 + g3 * 0.26)),
        Spawn { n: 270, by: Sm(1.0) }
            | Xa(lpx + lsid * (0.048 + l3 * 0.024) + sin(lth + l3 * 6.2831) * lrr)
            | Ya(lpy + 0.020 + cos(lth + l3 * 6.2831) * lrr * 1.28),
      ]
    | Jitter(0.0005)
    | Pin
    | Gradient(moon)
    | Tint(0.86, 1.0, 0.88, 0.75)
}

-- BELL → fireflies. Three a stroke, each a small lozenge. Everything else
-- in this forest is rooted; these are the only marks with anywhere to go,
-- and the wgsl below gives them one shared drifting field, so they wander
-- together the way air moves in a closed place.
draw firefly = {
    let idx = floor(Stroke * 119.0 + 0.5)
    let fi  = floor(idx / 24.0)
    let rem = idx - fi * 24.0
    let ri  = floor(rem / 12.0)
    let th  = ((rem - ri * 12.0) / 12.0) * 6.28318

    let bph = Note.t / 2.4 - floor(Note.t / 2.4)
    let fs  = Count * 0.61803 + fi * 0.38197
    let f1  = fs - floor(fs)
    let fs2 = fs * 3.3301
    let f2  = fs2 - floor(fs2)

    let px  = bph * 3.40 - 1.70 + (fi - 2.0) * 0.11 + (f1 - 0.5) * 0.09
    let py  = 0.22 + f2 * 1.72
    let r   = (0.010 + Note.z * 0.018) * (1.0 - ri * 0.45)

    Point
    | Spawn { n: 120, by: Sm(1.0) }
        | Xa(px + sin(th) * r)
        | Ya(py + cos(th) * r)
    | Jitter(0.0009)
    | Pin
    | Tint(1.0, 1.0, 0.92, 0.85)
}

-- BASS → the bed. One drift line per note lying flat in front of the
-- forest, between the far bank and your feet. Its PITCH is how far out it
-- lies — high notes ride up by the far bank, low notes come toward you —
-- and it is the one voice in the piece whose pitch actually moves, so it
-- is the one place pitch is allowed to place anything. The span is the
-- note's gain, not its length: `Note.l` here is 0.005, because a
-- sustained voice is sampled every 5 ms and its length is a tick.
draw drift = {
    let ws  = Count * 0.61803
    let w1  = ws - floor(ws)
    let wy  = 0.520 - (Note.y - 0.18) * 0.72
    let wx  = w1 * 3.34 - 1.67

    Point
    | Xa(wx)
    | Ya(wy)
    | Lerp { to: Direction(0.60 + Note.z * 1.38, 0, 0), n: 10 }
    | Modulate(Flow { scale: 2.2 }, 0.004)
    | Pin
    -- along the drift: pale where the moon reaches it, sinking to a dark
    -- earth as it runs under the trees. Named, so the sand's ramp and the
    -- light on every other body in the piece are the same fact.
    | Gradient(moon)
    -- and THINNER as it runs: full at the head of the line, trailing off
    -- at the tail, so a drift has a direction in substance and not only
    -- in tone.
    | Tint(1, 1, 1, 0.40 + 0.60 * (1.0 - Stroke))
}

-- ── THE VOICES ──────────────────────────────────────────────────────────

x = { Silence 1 }

-- the gankogui: two bells, one figure. Both draw fireflies, and the low
-- bell — the one that starts the cycle — burns brighter and lives longer,
-- so the downbeat is a lamp the others gather around.
bl = { Cowbell acoustic | Fm 2/3 | Gm 0.6
    | #bl
    | Color [#ffffe4, #f4ff9c, #c8ff5a, #6ee02c, #2c7a1a, #0a2408]
    | wgsl {
        Sm 0.030; Smx 1.1; Smy 1.1;
        Vm 0.0; Die 16;    -- the bell's sixteen strokes: the timeline, whole
        // it blinks. Each fly on its own phase, and the blink is what a
        // firefly IS — a steady dot would be a star.
        Bm "2.60 + Rand(2) * 1.40";
        Bm "0.62 + 0.38 * (1.0 - exp(0.0 - Time * 5.0))";
        Bm "0.05 + 0.95 * smoothstep(0.0, 0.55, life)";
        Za "0.02 - Rand(5) * 0.18";
        // ONE drifting field, read at each fly's own position: neighbours
        // get nearly the same answer and move as a body, far ones peel off
        Xa "0.055 * sin(y * 4.6 + Clock * 0.70) + 0.026 * sin(y * 10.5 - Clock * 0.48) + (0.62 * sin(Clock * 0.113) + 0.38 * sin(Clock * 0.071 + 1.7)) * 0.030";
        Ya "0.030 * cos(x * 3.8 - Clock * 0.62) + 0.018 * cos(x * 8.7 + Clock * 0.91)";
        Seq [ Direction(0, 0, 0) | Lm 6; ];
    }
    | firefly
    | glow
}

bh = { Cowbell acoustic | Gm 0.48
    | #bl
    | Color [#ffffe4, #e8ffb0, #a8f06a, #55c62a, #256614, #081c06]
    | wgsl {
        Sm 0.024; Smx 1.1; Smy 1.1;
        Vm 0.0; Die 16;    -- the same sixteen
        Bm "2.10 + Rand(6) * 1.20";
        Bm "0.62 + 0.38 * (1.0 - exp(0.0 - Time * 5.0))";
        Bm "0.05 + 0.95 * smoothstep(0.0, 0.55, life)";
        Za "0.02 - Rand(9) * 0.18";
        Xa "0.055 * sin(y * 4.6 + Clock * 0.70) + 0.026 * sin(y * 10.5 - Clock * 0.48) + (0.62 * sin(Clock * 0.113) + 0.38 * sin(Clock * 0.071 + 1.7)) * 0.030";
        Ya "0.030 * cos(x * 3.8 - Clock * 0.62) + 0.018 * cos(x * 8.7 + Clock * 0.91)";
        Seq [ Direction(0, 0, 0) | Lm 6; ];
    }
    | firefly
    | glow
}

-- the axatse: the loud stroke lays a whole clump, the ghost lays a few
-- blades. Neither moves once it is down — this is ground.
ax = { Shaker wsc | Gm 0.42 | Pa 1/4
    | #ax
    | Color [#fff4ea, #f6d9bd, #e0a87c, #b26a44, #6f3a26, #2a150f]
    | wgsl {
        Sm 0.030; Smx 1.0; Smy 1.8;
        Shade moon;
        Vm 0.0; Die 16;    -- the axatse's sixteen: one full turn
        -- THE WIND, the same field the shafts swing on, scaled by how
        -- far this mark stands above the ground: a mushroom is low and barely feels it
        Xa "(0.62 * sin(Clock * 0.113) + 0.38 * sin(Clock * 0.071 + 1.7)) * 0.004 * max(y - 0.30, 0.0)";
        Bm "1.20 + Rand(11) * 0.50";
        Bm "0.62 + 0.38 * (1.0 - exp(0.0 - Time * 5.0))";
        Bm "0.05 + 0.95 * smoothstep(0.0, 0.30, life)";
        Za "0.0 - 0.02 - Rand(12) * 0.10";
    }
    | mushroom
    | shallows
}

ag = { Shaker wsc | Gm 0.18 | Pa 1/4
    | #ax
    | Color [#f2fff2, #c4ecc8, #8cbf94, #56895f, #2f5236, #101f14]
    | wgsl {
        Sm 0.024; Smx 1.0; Smy 1.6;
        Shade moon;
        Vm 0.0; Die 16;    -- the same sixteen, ghosted
        -- THE WIND, the same field the shafts swing on, scaled by how
        -- far this mark stands above the ground: a pad on the water feels it least of all
        Xa "(0.62 * sin(Clock * 0.113) + 0.38 * sin(Clock * 0.071 + 1.7)) * 0.003 * max(y - 0.30, 0.0)";
        Bm "0.75 + Rand(13) * 0.35";
        Bm "0.62 + 0.38 * (1.0 - exp(0.0 - Time * 5.0))";
        Bm "0.05 + 0.95 * smoothstep(0.0, 0.30, life)";
        Za "0.0 - 0.02 - Rand(15) * 0.10";
    }
    | pad
    | shallows
}

-- kaganu: the understory. Cheapest figure, shortest life, furthest back —
-- it is the voice that fires most often and it has to be able to.
ka = { Tom conga | Fm 7/4 | Gm 0.38 | Pa -1/3
    | #ka
    | Color [#f4fff8, #c8f5dc, #8fe0b8, #4fae86, #266b52, #0d2b22]
    | wgsl {
        Sm 0.036; Smx 1.0; Smy 2.4;
        Shade moon;
        Vm 0.0; Die 16;    -- kaganu's sixteen: one full turn
        -- THE WIND, the same field the shafts swing on, scaled by how
        -- far this mark stands above the ground: reeds bend most of anything on the bank
        Xa "(0.62 * sin(Clock * 0.113) + 0.38 * sin(Clock * 0.071 + 1.7)) * 0.015 * max(y - 0.30, 0.0)";
        Bm "1.15 + Rand(13) * 0.50";
        Bm "0.62 + 0.38 * (1.0 - exp(0.0 - Time * 5.0))";
        Bm "0.05 + 0.95 * smoothstep(0.0, 0.40, life)";
        Za "0.0 - 0.26 - Rand(14) * 0.22";
        // a reed in a draught: it stiffens a little on the strike
    }
    | reed
    | bank
}

-- kidi: the vines. Slower than kaganu, longer-lived, and they hang still
-- — a liana that swayed on every stroke would be a rope in a gale.
ki = { Tom conga | Fm 5/4 | Gm 0.7 | Pa 1/6
    | #ki
    | Color [#fff4e4, #ffdcb0, #eeae74, #bd7842, #7d4a26, #2e1a0e]
    | wgsl {
        Sm 0.040; Smx 1.0; Smy 3.6;
        Shade moon;
        Vm 0.0; Die 12;    -- kidi's twelve: one full turn
        -- THE WIND, the same field the shafts swing on, scaled by how
        -- far this mark stands above the ground: a fern on a long stalk is the most flexible thing here
        Xa "(0.62 * sin(Clock * 0.113) + 0.38 * sin(Clock * 0.071 + 1.7)) * 0.020 * max(y - 0.30, 0.0)";
        Bm "1.45 + Rand(16) * 0.60";
        Bm "0.62 + 0.38 * (1.0 - exp(0.0 - Time * 5.0))";
        Bm "0.08 + 0.92 * smoothstep(0.0, 0.30, life)";
        Za "0.0 - 0.10 - Rand(17) * 0.18";
        // the whole vine leans, very slowly, all of it together
    }
    | crozier
    | thicket
}

-- sogo: the flowers. Big, slow, few — the low drum gets the largest single
-- body in the picture, and it sweeps open from its heart on the strike
-- and settles back, the way a night flower does.
so = { Tom acoustic | Fm 5/6 | Gm 0.8 | Pa -1/6
    | #so
    | Color [#ffffff, #fff2d6, #ffd9a0, #f0a8c8, #b06a9c, #40204a]
    | wgsl {
        Sm 0.040;
        Smx 1.0; Smy 2.4;
        Shade moon;
        Vm 0.0; Die 24;    -- sogo's twenty-four: one full turn
        -- THE WIND, the same field the shafts swing on, scaled by how
        -- far this mark stands above the ground: the canopy rides it
        Xa "(0.62 * sin(Clock * 0.113) + 0.38 * sin(Clock * 0.071 + 1.7)) * 0.016 * max(y - 0.30, 0.0)";
        Bm "1.05 + Rand(19) * 0.45";
        Bm "0.62 + 0.38 * (1.0 - exp(0.0 - Time * 5.0))";
        Bm "0.10 + 0.90 * smoothstep(0.0, 0.28, life)";
        Za "0.0 - 0.05 - Rand(20) * 0.14";
        // it dips on the strike and comes back up
    }
    | bloom
    | blooms
}

-- the anchor: the trees. Nothing is said about light here, so a trunk
-- takes every light in the piece — the moon on its shoulder, the canopy
-- in its shadow, and the floor's own glow on the buttress at the water.
bd = { Kick acoustic | Fm 2/3 | Gm 1
    | #bd
    | Color [#95b6cc, #46605e, #1e3128, #0f1c15, #080f0b, #040705]
    | wgsl {
        Sm 0.034; Smx 0.9; Smy 4.2;
        Vm 0.0; Die 24;    -- the anchor's twenty-four: one full turn
        -- THE WIND, the same field the shafts swing on, scaled by how
        -- far this mark stands above the ground: a trunk hardly bends, but its crown does
        Xa "(0.62 * sin(Clock * 0.113) + 0.38 * sin(Clock * 0.071 + 1.7)) * 0.008 * max(y - 0.30, 0.0)";
        Bm "0.62 + Rand(21) * 0.28";
        Bm "0.62 + 0.38 * (1.0 - exp(0.0 - Time * 5.0))";
        // a trunk does not blink out at Die — it goes back into the dark
        Bm "0.16 + 0.84 * smoothstep(0.0, 0.22, life)";
        Za "0.0 - 0.02 - Rand(22) * 0.10";
    }
    | trunk
    | boles
}

bell = {
    Seq [
        bl, x, x, bh, x, x, bh, x,
        x, x, bh, x, bh, x, x, x
    ]
}

kaganu = {
    Seq [
        x, ka, ka, x, x, ka, ka, x,
        x, ka, ka, x, x, ka, ka, x
    ]
}

axatse = {
    Seq [
        ax, ag, ag, ax, ag, ag, ax, ag,
        ag, ag, ax, ag, ax, ag, ag, ag
    ]
}

kidi = {
    Seq [
        ki, x, ki, ki | Gm 0.7, x, x, ki, x,
        ki | Gm 0.5, x, x, x
    ]
}

sogo = {
    Seq [
        so, x, x, x, x, x, x, so | Gm 0.7,
        x, x, x, x, so, x, x, x,
        x, x, x, so | Gm 0.6, x, x, x, x
    ]
}

anchor = {
    Seq [
        bd, x, x, x, x, x, x, x,
        x, bd | Gm 0.7, x, x, x, x, bd | Gm 0.8, x,
        x, x, x, bd | Gm 0.6, x, x, x, x
    ]
}

bass_tone = {
    Overlay [
        { 1/1, 0, 1, 0 },
        { 1/1, 1, 1/2, 0 },
        { 1/2, 0, 1/2, 0 },
        { 2/1, 0, 1/4, 0 },
        { 3/1, 0, 1/10, 0 },
    ]
}

fig_plain = {
    bass_tone | Seq [Fm 1 | Lm 2, Fm 3/2 | Lm 2, Fm 2 | Lm 2, Fm 6/5 | Lm 2 ]
}

fig_rise = {
    bass_tone | Seq [Fm 1 | Lm 2, Fm 6/5 | Lm 2, Fm 3/2 | Lm 2, Fm 1 | Lm 2 ]
}

fig_lean = {
    bass_tone | Seq [Fm 1 | Lm 3, Fm 3/2 | Lm 1, Fm 6/5 | Lm 2, Fm 3/2 | Lm 2 ]
}

fig_walk = {
    bass_tone | Seq [Fm 1 | Lm 1, Fm 3/2 | Lm 1, Fm 2 | Lm 2, Fm 6/5 | Lm 2, Fm 4/3 | Lm 2 ]
}

fig_fall = {
    bass_tone | Seq [Fm 2 | Lm 2, Fm 3/2 | Lm 1, Fm 7/6 | Lm 1, Fm 1 | Lm 4 ]
}

cadence = {
    bass_tone | Seq [Fm 3/2 | Lm 2, Fm 9/8 | Lm 2, Fm 1 | Lm 4 ]
}

cell_a = {
    Seq [
        fig_plain,
        fig_rise | Fm 5/6,
    ]
}

cell_b = {
    Seq [
        fig_lean | Fm 10/9,
        fig_walk | Fm 40/27
        | Overlay [
            Fm 0,
            { 1/1, 0, 1, 0 },
            { 3/4, 1, 1, -1 },
            { 9/16, 1, 1, 1 },
        ]
    ]
}

cell_c = {
    Seq [
        fig_fall | Fm 80/81
        | Overlay [
            { 2/1, 0, 1, 1 },
            { 1/1, 1, 1, -1 },
        ],
        cadence | Fm 80/81 | Overlay [Fm 1, Fm 1/2],
    ]
}

bass = {
    Seq [cell_a, cell_b, cell_c]
    | Fm 2
    | Tanh(2.2, 0.6)
    | Gm 0.55
    -- sand is not one colour either: bleached where the gap in the canopy
    -- lets the moon down onto it, sinking to a damp earth as it runs out
    -- under the trees. A ramp along the drift, which `Gradient` in `drift`
    -- reads across each line.
    | Color [#f6ecda, #ddc9a8, #b6a07c, #877457, #574a37, #221c15]
    | wgsl {
        -- THIN. `Smx` stretches these into lines and `Smy` is their
        -- thickness, so thinning is Smy's job, not Sm's — dropping Sm
        -- would shorten the line as well as narrow it, and the length is
        -- the current.
        Sm 0.19; Smx "2.6 + (0.0 - y) * 1.8"; Smy 0.62;
        Vm 0.04; Die 13;   -- 2.0 s
        Za "0.0 - 0.02 - Rand(23) * 0.10";
        Bm "0.55 + Rand(24) * 1.15";
        Bm "0.62 + 0.38 * (1.0 - exp(0.0 - Time * 5.0))";
        Bm "0.08 + 0.92 * smoothstep(0.0, 0.20, life)";
        Seq [ Direction(0.018, 0, 0) | Lm 12; ];
    }
    | drift
    | sand
}

spiral = {
    Overlay [
        bell | Repeat 3,
        kaganu | Repeat 3,
        axatse | Repeat 3,
        kidi | Repeat 4,
        sogo | Repeat 2,
        anchor | Repeat 2
    ]
}

intro = { Overlay [bell | Repeat 3, axatse | Repeat 3] }

build = {
    Overlay [
        bell | Repeat 3, kaganu | Repeat 3,
        axatse | Repeat 3, kidi | Repeat 4
    ]
}

strip = {
    Overlay [
        bell | Repeat 3,
        axatse | Repeat 3,
        bass
    ]
}

deep = {
    Overlay [
        spiral | Repeat 4,
        Seq [
            bass,
            bass | Fm 80/81,
            bass | Fm 6400/6561,
            bass | Fm 512000/531441,
        ]
    ]
}

hollow = {
    Overlay [
        bell | Repeat 3,
        kaganu | Repeat 3,
        bass
    ]
}

main = {
    Seq [
        strip,
        deep,
        hollow
    ]
    | Fm 3/2
} -> [bd, so, ki, ka, ax, bl]

-- ════════════════════════════════════════════════════════════════════════
-- NOTES — known weaknesses, for the next pass
--
-- 1. THE SURFACES DON'T RIDE THEIR BODIES. Bark, dew and fungus are all
--    screen-space grids masked by coverage. Vertical bark on a vertical
--    trunk gets away with it; the dew on a petal does not, and swims when
--    anything moves. The fix is surface as GEOMETRY — emits of the
--    figure's own draw — which would also make it notes rather than
--    texture.
--
-- 2. THREE PLACES KNOW THE BAR. Every draw computes `Note.t / 2.4` for
--    itself, and 2.4 is the bell's sixteen strokes at l = 3/20 written out
--    by hand. Change `l` and the ruler silently detaches from the music.
--    It wants to be one named length.
--
-- 3. TWO PLACES KNOW THE WATERLINE. The draws seat the trees at y = 0.556
--    and the stream's Raw hard-codes the same line as uv.y = 0.70.
--
-- 4. ONLY THE TRUNKS REFLECT. One reference slot per def, so the water
--    taps `boles` only — the flowers and the fireflies cast nothing. The
--    fireflies are the ones worth it: a lamp with no reflection reads as
--    a scratch on the print. Fix by compositing both into an intermediate
--    def and referencing that.
--
-- 5. THE BAR RULER HAS NO EDGE. Nothing in the frame marks x = 0, so the
--    downbeat is only legible as the moment several voices light the same
--    column. A very faint standing mark at the turn of the bar — one reed,
--    one break in the water — would give the eye the barline it is
--    already looking for.
--
-- 6. EVERY VOICE IS SAMPLED THIRTY TIMES. A 0.15 s note is 30 op4d events
--    at 5 ms, and each one runs the whole draw again. `Note.t` is used as
--    a sub-cell dither so the copies interleave instead of stacking, which
--    turns the waste into fill — but it is still waste, and it is why the
--    per-note emit counts here are a tenth of what they look like they
--    should be.
--
-- 7. NOTHING MOVES IN DEPTH. Every plane sits at a fixed Za. The parallax
--    that would sell the layering is available (Za is already randomised
--    per mark) but nothing moves the camera.
-- ════════════════════════════════════════════════════════════════════════
